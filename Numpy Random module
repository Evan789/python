import numpy as np

name_list = ['Evan', 'Sern', 'Yuan', 'Mike jin']

d = np.random.choice(name_list, size = (3,3), p = (0.5, 0.2, 0.1, 0.2))

print(d)

output:
[['Yuan' 'Evan' 'Mike jin']
 ['Evan' 'Mike jin' 'Evan']
 ['Sern' 'Evan' 'Mike jin']]
